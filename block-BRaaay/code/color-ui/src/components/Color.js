import data from "../colors.json"

function Color() {
    return <section className="container">
        {
            [data].map((color)=> {
                return ;
            })
        }
    </section>
}

export default Color;